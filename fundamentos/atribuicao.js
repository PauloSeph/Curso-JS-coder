const a = 7
let b = 3

// Atribuiçao aditiva, Equivalente --> b = b + a
b += a
console.log(b)

// Atribuição subtrativa, --> b = b - 4 
b -= 4  // 
console.log(b)

// Atribuiçao multiplicativa. Equivalente b = b * 2
b *= 2  
console.log(b)

// Divisão. Equivalente  b = b / 2
b /= 2 
console.log(b)

// O que sobrou do resto da divisão
b %= 4 
console.log(b)